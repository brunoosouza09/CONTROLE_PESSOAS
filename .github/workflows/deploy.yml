name: Deploy com Docker

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  main:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v3

      - name: Verificar e configurar secrets
        run: |
          echo "🔍 Verificando secrets do repositório..."
          set +e
          
          # Verificar CLOUD_SSH_KEY (ou SSH_KEY como fallback)
          if [ -n "${{ secrets.CLOUD_SSH_KEY }}" ]; then
            SSH_KEY_VALUE="${{ secrets.CLOUD_SSH_KEY }}"
            SSH_KEY_LEN=${#SSH_KEY_VALUE}
            echo "✅ CLOUD_SSH_KEY encontrado (tamanho: ${SSH_KEY_LEN} caracteres)"
          elif [ -n "${{ secrets.SSH_KEY }}" ]; then
            SSH_KEY_VALUE="${{ secrets.SSH_KEY }}"
            SSH_KEY_LEN=${#SSH_KEY_VALUE}
            echo "✅ SSH_KEY encontrado (tamanho: ${SSH_KEY_LEN} caracteres)"
          else
            echo "❌ Secret SSH_KEY/CLOUD_SSH_KEY não configurado ou vazio!"
            echo "💡 Verifique se o secret existe em: Settings > Secrets and variables > Actions"
            exit 1
          fi
          
          # Verificar CLOUD_HOST (ou SSH_HOST como fallback)
          if [ -n "${{ secrets.CLOUD_HOST }}" ]; then
            SSH_HOST_VALUE="${{ secrets.CLOUD_HOST }}"
            echo "✅ CLOUD_HOST: $SSH_HOST_VALUE"
          elif [ -n "${{ secrets.SSH_HOST }}" ]; then
            SSH_HOST_VALUE="${{ secrets.SSH_HOST }}"
            echo "✅ SSH_HOST: $SSH_HOST_VALUE"
          else
            echo "❌ Secret SSH_HOST/CLOUD_HOST não configurado ou vazio!"
            echo "💡 Configure com o IP da VPS: 191.252.214.59"
            exit 1
          fi
          
          # Configurar SSH_USER (CLOUD_USER ou SSH_USER ou root como padrão)
          if [ -n "${{ secrets.CLOUD_USER }}" ]; then
            SSH_USER_VALUE="${{ secrets.CLOUD_USER }}"
            echo "✅ CLOUD_USER: $SSH_USER_VALUE"
          elif [ -n "${{ secrets.SSH_USER }}" ]; then
            SSH_USER_VALUE="${{ secrets.SSH_USER }}"
            echo "✅ SSH_USER: $SSH_USER_VALUE"
          else
            SSH_USER_VALUE="root"
            echo "ℹ️ Usando padrão: root"
          fi
          
          echo "✅ Todos os secrets necessários estão configurados!"
          set -e

      - name: Adiciona chave SSH
        run: |
          mkdir -p ~/.ssh
          # Usar CLOUD_SSH_KEY se existir, senão SSH_KEY
          if [ -n "${{ secrets.CLOUD_SSH_KEY }}" ]; then
            echo "${{ secrets.CLOUD_SSH_KEY }}" > key.pem
          else
            echo "${{ secrets.SSH_KEY }}" > key.pem
          fi
          chmod 600 key.pem
          # Usar CLOUD_HOST se existir, senão SSH_HOST
          if [ -n "${{ secrets.CLOUD_HOST }}" ]; then
            ssh-keyscan -H ${{ secrets.CLOUD_HOST }} >> ~/.ssh/known_hosts 2>/dev/null || true
          else
            ssh-keyscan -H ${{ secrets.SSH_HOST }} >> ~/.ssh/known_hosts 2>/dev/null || true
          fi

      - name: Deploy via SSH com Docker
        run: |
          set -e
          # Determinar valores dos secrets
          if [ -n "${{ secrets.CLOUD_USER }}" ]; then
            SSH_USER="${{ secrets.CLOUD_USER }}"
          elif [ -n "${{ secrets.SSH_USER }}" ]; then
            SSH_USER="${{ secrets.SSH_USER }}"
          else
            SSH_USER="root"
          fi
          
          if [ -n "${{ secrets.CLOUD_HOST }}" ]; then
            SSH_HOST="${{ secrets.CLOUD_HOST }}"
          else
            SSH_HOST="${{ secrets.SSH_HOST }}"
          fi
          
          echo "🔐 Conectando como $SSH_USER@$SSH_HOST"
          echo "🔑 Usando chave SSH: key.pem"
          echo "🌐 Host: $SSH_HOST"
          
          # Testar conexão primeiro
          ssh -i key.pem -o StrictHostKeyChecking=no -o ConnectTimeout=10 -o BatchMode=yes ${SSH_USER}@${SSH_HOST} echo "✅ Teste de conexão SSH OK" || {
            echo "❌ Falha na conexão SSH"
            echo "💡 Verifique:"
            echo "  - Chave SSH está correta?"
            echo "  - IP/Host está correto?"
            echo "  - Porta 22 está aberta?"
            exit 1
          }
          
          ssh -i key.pem -o StrictHostKeyChecking=no -o ConnectTimeout=10 ${SSH_USER}@${SSH_HOST} << 'EOF'
            set -e
            echo "📁 Acessando diretório..."
            cd /root/CONTROLE_PESSOAS || { echo "❌ Diretório não encontrado"; exit 1; }
            echo "📥 Fazendo pull do código..."
            git pull origin main || { echo "❌ Falha no git pull"; exit 1; }
            echo "🛑 Parando containers existentes..."
            docker compose down || true
            echo "🔨 Construindo e iniciando containers..."
            docker compose up -d --build || { echo "❌ Falha ao iniciar containers"; exit 1; }
            echo "⏳ Aguardando containers iniciarem..."
            sleep 15
            echo "✅ Verificando status dos containers..."
            docker compose ps
            echo "📋 Verificando logs do App..."
            docker compose logs app --tail=30
            echo "✨ Deploy finalizado com sucesso!"
          EOF
